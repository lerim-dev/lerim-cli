# Dashboard Frontend Sources

Source projects that compile dashboard bundles.

- `graph-explorer/`: TypeScript + Vite project that builds the graph explorer assets consumed by `dashboard/index.html`.

Build flow:
- `cd dashboard/frontend/graph-explorer`
- `npm install`
- `npm run build`
